<!DOCTYPE html>
<html>
<% var opts = htmlWebpackPlugin.options, data = opts.indexData; %>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
    <title>Linux Remote</title>
  </head>
  <body>
  <div id="lr-app"><span>Loading...</span></div>
  <script>
    if(navigator.userAgent.indexOf('Chrome') === -1 || 
      navigator.vendor.indexOf('Google Inc')=== -1){ //stackoverflow #4565112
        document.body.innerHTML = '<h1>Sorry! This project only support Chrome now.</h1>';
        throw new Error('This project only support Chrome.');
    }
  </script>
  <% opts.nodeModuleStatic.forEach(function(v){%>
  <script src="<%=v%>"></script><% }) %>
  
  <script>
    var SERVER_CONFIG = <%=JSON.stringify(data)%>
  </script>
</body>
</html>
